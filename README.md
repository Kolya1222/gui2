# Код к заданию 1

```python
import sys
a=int(input("Введите первое число: "))
b=int(input("Введите второе число: "))
c=int(input("Введите третье число: "))
if (0>c) or (0>b) or (0>a):
    print("Треугольник несуществует")   
    sys.exit()
if ((a+b)>c) and ((a+c)>b) and ((b+c)>a):
    print ("Треугольник существует")
else:
    print("Треугольник несуществует")
    sys.exit()
if (a==b and a==c and b==c):
    print ("Все стороны равны. Треугольник Равносторонний") 
    sys.exit()
if (a==b and a!=c) or (a==c and a!=b) or (b==c and b!=a):
   print ("Треугольник Равнобедренный") 
   sys.exit()
print ("Треугольник Разносторонний")
sys.exit()
```
## Таблица к заданию 1 (задание 2)

| A | B | C | Ожидаемый результат | Объект проверки |
| - | - | - | ------------------- | --------------- |
| 0 | 1 | 2 | Треугольник несуществует | Ввод нулевого значения |
| -1 | 1 | 2 | Треугольник несуществует | Ввод отрицательного значения |
| 1 | 1 | 5 | Треугольник несуществует | Сумма двуч чисель меньше третьего |
| 1 | 1 | 1 | Треугольник Равносторонний | Проверка на равностороность треугольника |
| 3 | 3 | 2 | Треугольник Равнобедренный | Проверка на Равнобедренность треугольника |
| 3 | 5 | 7 | Треугольник Разносторонний | Проверка на Равнобедренность треугольника |

# Код к заданию 2

```python
import sys
a=int(input("Введите первое число: "))
b=int(input("Введите второе число: "))
c=int(input("Введите третье число: "))
if (0>c) or (0>b) or (0>a):
    print("Треугольник несуществует")   
    sys.exit()
if ((a+b)>c) and ((a+c)>b) and ((b+c)>a):
    print ("Треугольник существует")
else:
    print("Треугольник несуществует")
    sys.exit()
max = 0
if (a>b):
    if(a>c):
        max=a
        a=b
        b=c
    else:
        max=c
else:
    if (b>c):
        max=b
        b=c
    else:
        max=c
print("Максимальная сторона:"+str(max))
if (a*a+b*b>c*c):
    print ("Треугольник остроугольный")
    sys.exit()
else:
    if (a*a+b*b<c*c):
        print ("Треугольник тупоугольный")
        sys.exit()
print ("Треугольник прямоугольный")
sys.exit()
```

## Таблица к заданию 3 (задание 2)

| A | B | C | Ожидаемый результат | Объект проверки |
| - | - | - | ------------------- | --------------- |
| 0 | 1 | 2 | Треугольник несуществует | Ввод нулевого значения |
| -1 | 1 | 2 | Треугольник несуществует | Ввод отрицательного значения |
| 1 | 1 | 5 | Треугольник несуществует | Сумма двуч чисель меньше третьего |
| 3 | 4 | 5 | Треугольник прямоугольный | Проверка на прямоугольность треугольника |
| 5 | 9 | 10 | Треугольник остроугольный | Проверка на остроугольность треугольника |
| 3 | 5 | 7 | Треугольник тупоугольный | Проверка на тупоугольность треугольника |

# Задание 4
Во время теста 1 учитывается ноль.
**Рекомендации:** Измените условие проверки на отрицательные числа и ноль: if a <= 0 or b <= 0 or c <= 0:.
**Рекомендации по коду к заданию 3** Вместо сложных вложенных if для поиска максимума, проще всего отсортировать список сторон
```python
sides = sorted([a, b, c])
a, b, c = sides[0], sides[1], sides[2]
```
